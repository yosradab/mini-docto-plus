# Copy to .env.development (or .env.local) and adjust if needed.
# Default dev setup uses Vite proxy — see vite.config.js
VITE_API_URL=/api

# Direct backend URL (no proxy), e.g. production build:
# VITE_API_URL=http://localhost:5000/api
